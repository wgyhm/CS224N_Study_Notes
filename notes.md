### Lecture 1

[python学习](https://colab.research.google.com/github/cs231n/cs231n.github.io/blob/master/python-colab.ipynb#scrollTo=M-E4MUeVL9iC)
[Matplotlib](https://matplotlib.org/stable/gallery/index.html)
[奇异值分解SVD](https://zhuanlan.zhihu.com/p/29846048)

注意到如果连不上imdb_dataset,可以设置系统变量，但无论是 HTTP_PROXY 还是 HTTPS_PROXY = 'http://xxx:xxx'，不是 https

### Lecture 2

做一点简单的辨析。

* Task1 中的 co-occurence embedding + SVD，虽然直观，但是矩阵很大，处理大语料不够高效，无法自然使用线性模型
* word2vec 的概率函数（Skip-gram）：通过概率目标函数让模型自动调整向量，使其保持“语义相似词在向量空间也相近”。高效，可在大语料上训练；自然得到低维稠密向量

* 1.3 求 loss 为什么要平方？
  * 首先，算损失函数的本质上是在让模型预测和“真实统计量”尽量接近
  * 平方的意义是防止正负相互抵消。
  * 之后取对数，防止某些 $X_{i,j}$ 差距过大，减小其差距。
* 全局交叉熵损失要做归一化，所以改成了代价更小的最小二乘目标。
* 算出损失以后，模型会根据根据 J 对参数求梯度，再修改参数。
* Intrinsic evaluation 是在中间子任务上快速评估 embedding 质量；Extrinsic evaluation 是在真实下游任务上评估整体效果，通常更慢、更贵。
* 3.1 把很多 NLP 下游任务写成分类问题，并先从线性分类器出发。课上讲的就是归类为 location/no-location
* 3.2 的 retraining words，就是在下游任务上继续微调预训练词向量；数据大时可能有帮助，数据小时可能有害。
* 3.3 里 y 是 one-hot 标签，表示在不在那个类里，所以每个分量是 0/1；C 表示总类别数；θ 是所有可训练参数，不是超参数。
* 3.4 不是只讨论 window 大小，而是在说要把“中心词 + 上下文词向量”组成。把这些词向量拼成的一个大输入向量。后面反向传播时，对这个大向量求梯度，再把梯度分发回每个位置对应的词向量。
3.5 才正式说明：线性分类器不够时，需要非线性分类器，比如神经网络。

### Lecture 3

* 2.3 Dropout 中

  **训练时的正向传播**：随机丢弃一部分节点

  **训练时的反向传播**：只给刚才保留下来的那些节点传梯度

  **测试/预测时的正向传播**：使用全部节点

* dropout 是一种 regularization，能减少 overfitting，并且可以理解成“同时训练了指数多个更小的网络，再对它们的预测做平均”。

* 为什么测试时又要用全部节点？因为测试时我们不想再引入随机性了。预测结果要稳定，所以要用完整网络。

* 为什么测试时还要缩放？因为某神经元正常输出是 $h$，训练时以概率 $p$ 保留，概率 $1-p$ 变成 0，那它训练时的期望输出就是 $p h$。所以应当做一个缩放，使测试时神经元的期望输出和训练时近似一致。

* 2.5 Whitening 中 
  **变换协方差矩阵等于单位矩阵**。
   这意味着：**各个特征之间互不相关，而且每个特征的方差都等于 1。**让每个特征互不相关。

* **方差**：看一个特征自己波动有多大

  **协方差**：看两个特征是不是一起变大、一起变小

  **协方差矩阵**：把“每一对特征之间的协方差”全部整理成一个矩阵
  
  定义行为样本，列是特征：
  
  **协方差矩阵的标准公式**是
  $$
  \Sigma=\frac{1}{n}\sum_{i=1}^n (x^{(i)}-\mu)(x^{(i)}-\mu)^T
  $$
  或者写成矩阵形式
  $$
  \Sigma=\frac{1}{n}X_c^T X_c
  $$
  其中 $X_c$ 表示**已经做过去均值（centered）**的数据矩阵。
  
  在 $A$ 已经中心化，并且差一个常数因子时，$\Sigma=\frac{1}{n}A^TA$ 才对应协方差矩阵。
  
* 具体处理方法：先对去均值后的 $X_0$ 做 SVD 得到 $U,S,V$，再用 $VX_0$ 把数据投影到由 $U$ 的列向量定义的新基底里，最后按 $S$ 里的奇异值逐维缩放，使特征变得不相关且方差为 1。

* $V$ 提供了一个新的坐标系：这个坐标系对齐了数据最主要的变化方向；$S$ 告诉你每个方向上数据“伸得有多长”。

* $A^TA$中计算的便是 n 阶的协方差矩阵，每一个值代表着原来的 n 个特征之间的相关性。当对这个协方差矩阵进行特征分解之后，我们可以得到奇异值和右奇异矩阵，而这个右奇异矩阵则是一个新的坐标系，奇异值则对应这个新坐标系中每个基对于整体数据的影响大小，我们这时便可以提取奇异值最大的k个基，作为新的坐标，这便是PCA的原理。
  右奇异矩阵应该是固定点的数量，压缩冗余的特征，左奇异矩阵是固定这么多特征，去掉冗余的点。

* **右奇异矩阵 $V$**：定义了**行空间 / 输入空间 / 特征空间**中的一组正交方向。也就是“原始特征应该沿哪些方向重新组合来看”。

  **左奇异矩阵 $U$**：定义了**列空间 / 输出空间 / 样本映射后空间**中的一组正交方向。也就是“这些输入方向经过 $A$ 作用后，在输出空间里对应到哪些方向”。

  **奇异值 $\Sigma$**：表示每个对应方向被拉伸或压缩的强度

* U 对应了行空间那一侧，V 对应列空间那一侧。如果定义行为特征，列为样本，就像 note 里面用 $X'=UX_0$ 。

### Lecture 4

### PyTorch Tutorial Session

`hidden_dim`，控制隐藏层有多宽，也就是模型中间表示的维度大小

`freeze_embeddings`控制的是，也就是词向量参数是否更新

### Assignment 2

添加了用 cuda 跑的版本。推荐下载的 pytorch 版本不支持 sm120，可能是 24年 还有没有这东西。

具体到写代码就能学很多东西。还是要多写代码。

* 为什么每次要调用 batch_size? 
  
  提高预测效率。

* sofamax 可以理解为将一组分数转为概率分布
* hidden 层用于学表示，提取特征，logits 层用于把特征变成分数表示。
  
* 为什么 hidden 和 logits 不能单纯的看成多学一层？因为 ReLU 不是线性变换。
### Lecture 5&6

bidirectional RNNs：单向 RNN 适合“按时间往前生成”；双向 RNN 适合“在整句已知时理解当前位置”.
